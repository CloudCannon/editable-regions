# The editable-regions Hugo module intentionally contributes no
# configuration. Everything it needs is template- and asset-level:
#
#   layouts/partials/cc/   annotation helpers and the bundle pipeline
#   assets/                the prebuilt runtime and Hugo renderer WASM
#
# Consuming sites add one partial to their <head> and nothing else:
#
#   {{ partial "cc/live-editing-head.html" . }}
#
# Options live under params.editable_regions (see README.md).
