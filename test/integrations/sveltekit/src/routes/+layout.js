// Trailing slashes keep prerendered URLs aligned with the nav hrefs and the
// collection `url: "/[slug]/"` in cloudcannon.config.yml.
export const prerender = true;
export const trailingSlash = "always";
