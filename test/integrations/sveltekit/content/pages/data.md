---
title: Data file editable regions
---

Everything on this page is backed by a file in `data/`, reached through
`@data[key]`, rather than by this page's own frontmatter. The same data drives
the header and footer, so an edit here should update those too.
