---
title: Editable regions 11ty test
layout: page-shell
---
<p>
	Each page below exercises a different live-editing surface. Pages render
	server-side via Eleventy and load <code>/register-components.js</code> so the
	browser bundle is wired up too.
</p>

<nav>
	<ul>
		<li><a href="/filters/">Filters</a> — auto-mirror + closure + async + override + built-in</li>
		<li><a href="/shortcodes/">Shortcodes</a> — plain + paired + closures + async</li>
		<li><a href="/custom-tags/">Custom tags</a> — auto-mirrored <code>echo</code> + <code>includeWith</code></li>
		<li><a href="/render-plugin/">RenderPlugin shims</a> — <code>renderTemplate</code> / <code>renderFile</code> / <code>renderContent</code></li>
		<li><a href="/globals/">Globals</a> — <code>eleventy</code>, <code>page</code>, <code>collections</code>, <code>process.env</code></li>
		<li><a href="/unsupported/">Unsupported filters</a> — warn-once stubs</li>
		<li><a href="/location-probe/">Location probe</a> — diagnostic for what the browser sees inside CC</li>
	</ul>
</nav>
