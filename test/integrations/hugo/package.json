{
	"name": "hugo-editable-regions-fixture",
	"type": "module",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build": "npm --prefix ../../.. run build:hugo && hugo --cleanDestinationDir --quiet && node verify-bundle.mjs"
	}
}
