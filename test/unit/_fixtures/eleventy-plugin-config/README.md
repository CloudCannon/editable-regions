# Eleventy plugin-config fixture

Built by `test:build-eleventy-plugin-config`. Runs eleventy multiple times with
different `--config` files, each exercising a different `pluginOptions`
combination. Output goes to separate `_site/<scenario>/` directories.
